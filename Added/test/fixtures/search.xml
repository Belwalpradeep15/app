<?xml version="1.0" encoding="UTF-8"?>
<Search xmlns="http://www.fotechsolutions.com/schemas/repository-0.1.xsd">
    <Target id="1" type="FibreLine">
        <% if params.key?(:suppress_route) or params.key?(:add_map_calibration) %>
          <Options>
              <% if params.key?(:suppress_route) %>
              <Suppress field="Route"/>
              <% end %>
              <% if params.key?(:add_map_calibration) %>
              <Add field="MapCalibration"/>
              <% end %>
          </Options>
        <% end %>
        <% if params.key?(:filter) 
             filter  = params[:filter] %>
              <Filters>
                <Filter field="<%= filter[:field] %>" operation="<%= filter[:operation] %>"><%= filter[:value] %></Filter>
              </Filters>
        <% end %>
    </Target>
</Search>
